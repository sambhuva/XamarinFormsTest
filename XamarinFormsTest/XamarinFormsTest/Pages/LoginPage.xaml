<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsTest.Pages.LoginPage" BindingContext="{Binding Source={StaticResource Locator}, Path=Login}">
    <StackLayout>
        <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />
        <Entry Placeholder="User Name" Text="{Binding UserName}"></Entry>
        <Entry IsPassword="True" Placeholder="Password" Text="{Binding Password}"></Entry>
    </StackLayout>
</ContentPage>